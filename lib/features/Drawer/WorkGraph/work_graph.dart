import 'package:calander/features/Drawer/WorkGraph/presentation/view/work_graph_screen.dart';
import 'package:flutter/material.dart';

class WorkGraph extends StatelessWidget {
  const WorkGraph({super.key});
  static const String NavId = "WorkGraph";

  @override
  Widget build(BuildContext context) {
    return const WorkGraphScreen();
  }
}
